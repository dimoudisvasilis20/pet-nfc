PORT=3000

SESSION_SECRET=change-me-to-a-long-random-string

# Either set DATABASE_URL (preferred on most hosts, e.g. Render/Railway/Neon)...
DATABASE_URL=

# ...or set these individually (used for local development):
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pet_nfc
DB_USER=postgres
DB_PASSWORD=
